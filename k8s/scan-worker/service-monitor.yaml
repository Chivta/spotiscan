apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: scan-worker
  namespace: ruscan
  labels:
    app: scan-worker
spec:
  selector:
    matchLabels:
      app: scan-worker
  endpoints:
    - port: http
      path: /metrics
      interval: 15s